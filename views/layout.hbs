<!DOCTYPE html>
<head>
    <title></title>
    <style>
        /* Add a black background color to the top navigation */
        .topnav {
            background-color: #333;
            overflow: hidden;
        }

        /* Style the links inside the navigation bar */
        .topnav a {
            float: left;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 17px;
        }

        /* Change the color of links on hover */
        .topnav a:hover {
            background-color: #ddd;
            color: black;
        }

        /* Add a color to the active/current link */
        .topnav a.active {
            background-color: #04AA6D;
            color: white;
        }

        .body {
            margin: 0;
            padding: 0;
            font-family: 'Fira Sans Extra Condensed', sans-serif;
            box-sizing: content-box;
            background-color: #D0CCD0;
            letter-spacing: 0.05em;
            color: #050311;
        }

        table {
            border-collapse: collapse;
            margin: auto;
            font-size: 0.9em;
            min-width: 400px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.15);
        }

        thead tr {
            background-color: #04AA6D;
            color: #ffffff;
            text-align: left;
        }

        th, td {
            padding: 12px 15px;
            text-align: left;
        }

        tbody tr {
            border-bottom: 1px solid #dddddd;
        }

        tbody tr:nth-of-type(even) {
            background-color: #f3f3f3;
        }

        tbody tr:last-of-type {
            border-bottom: 2px solid #04AA6D;
        }

        .form-tag {
            margin-left: 100px;
        }

        form {
            margin-left: 100px;
        }

        .result {
            text-align: center;
        }

        .about {
            margin-left: 100px;
            margin-right: 100px;
        }

        .about h3 {
            text-align: center;
        }

        .about a {
            text-decoration: none;
            color: #57068c;
        }

        .rotate {
            animation: rotate 2s infinite linear;
        }
    </style>
</head>
<body>
<div>
    <div class="topnav">
        <a class="active" href="/">Gourmet Roulette</a>
        <a href="/list">Favorites</a>
        <a href="/about">About</a>
    </div>
    <div class="body">
        <br>
        <br>
        {{{body}}}
        <br>
        <br>
        <h3 style="text-align:center;">NYU CSCI-UA 467 (Applied Internet Technology) Final Project</h3>
        <br>
        <br>
    </div>
</div>
</body>
